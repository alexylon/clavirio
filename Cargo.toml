[package]
name = "ferrotype"
version = "0.1.0"
edition = "2021"

[dependencies]
crossterm = { version = "0.29", features = ["event-stream"] }
futures = "0.3"
ratatui = "0.30"
tokio = { version = "1", features = ["full"] }

[profile.release]
lto = true
